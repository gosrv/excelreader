// generate by excelreader

public class TableMgr {
    public static TableMgr ins
    {{range . -}}
    public Dictionary<string,{{.Name}}> table{{.Name}}
    {{end}}

    {{range . -}}
    public void load{{.Name}}() {
        data := TableDataLoad("{{.Name}}")
        itemArray := &{{.Name}}Array{}
        err := proto.Unmarshal(data, itemArray)
        if err != nil {panic(err)}
        this.table{{.Name}} = new Dictionary<string,{{.Name}}> table{{.Name}}
        for (i := 0; i < len(itemArray.Keys); i++) {
            this.table{{.Name}}[itemArray.Keys[i]] = itemArray.Items[i]
        }
    }
    {{end -}}

    public void Load() {
        {{range . -}}
        this.load{{.Name}}()
        {{end}}
    }
}



